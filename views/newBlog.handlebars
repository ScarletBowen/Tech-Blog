<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{username}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New Tech Blog:</h3>

    <form class="form new-blog-form">
      <div class="form-group">
        <label for="blog-title">Blog Title:</label>
        <input class="form-input" type="text" id="blog-title" name="title" />
      </div>
      <div class="form-group">
        <label for="blog-content">Blog Content:</label>
        <textarea class="form-input" id="blog-content" name="content"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="c-button c-button--brand">Create</button>
      </div>
    </form>
  </div>
{{!-- 
  {{#if blog.length}}
  <div class="col-md-6 project-list">
    <h3>Current Blogs:</h3>

    {{#each blogs }}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/blog/{{this.id}}">{{this.title}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger delete-btn" data-id="{{this.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{else}}
    <div class="col-md-6 project-list">
      <h3>No blogs yet.</h3>
      </div>
  {{/if}}
</div> --}}

<script src="/js/newblog.js"></script>
